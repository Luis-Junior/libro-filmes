import { ImgAdressPipe } from './img-adress.pipe';

describe('ImgAdressPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgAdressPipe();
    expect(pipe).toBeTruthy();
  });
});
